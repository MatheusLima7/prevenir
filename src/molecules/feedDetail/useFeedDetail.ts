import feedDetailConstants from "./feedDetail.constants";

export default () => {
  const { MORE_INFO, MORE_INFO_DESCRIPTION } = feedDetailConstants;

  return {
    MORE_INFO,
    MORE_INFO_DESCRIPTION,
  };
};
